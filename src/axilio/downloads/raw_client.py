# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.datetime_utils import serialize_datetime
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import encode_path_param
from ..core.parse_error import ParsingError
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..types.delete_file_output_body import DeleteFileOutputBody
from ..types.file_download_list_response import FileDownloadListResponse
from .types.downloads_list_request_order import DownloadsListRequestOrder
from .types.downloads_list_request_sort import DownloadsListRequestSort
from pydantic import ValidationError


class RawDownloadsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def list(
        self,
        *,
        q: typing.Optional[str] = None,
        mime_type: typing.Optional[str] = None,
        min_size_bytes: typing.Optional[int] = None,
        max_size_bytes: typing.Optional[int] = None,
        created_after: typing.Optional[dt.datetime] = None,
        created_before: typing.Optional[dt.datetime] = None,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        session_id: typing.Optional[str] = None,
        sort: typing.Optional[DownloadsListRequestSort] = None,
        order: typing.Optional[DownloadsListRequestOrder] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[FileDownloadListResponse]:
        """
        Returns one page of the files captured off phones during sessions (AXI-1449), newest first. Each row carries its capture state: a skipped or failed capture is a visible entry with a reason, not an absence. Downloads share the library and its storage quota with uploads, and a ready download can be delivered to a phone by its id like any upload.

        Parameters
        ----------
        q : typing.Optional[str]
            filter by filename, case-insensitive substring match

        mime_type : typing.Optional[str]
            only downloads of exactly this media type

        min_size_bytes : typing.Optional[int]
            only downloads at least this many bytes (0 = no bound)

        max_size_bytes : typing.Optional[int]
            only downloads at most this many bytes (0 = no bound)

        created_after : typing.Optional[dt.datetime]
            only downloads registered at or after this time (RFC 3339)

        created_before : typing.Optional[dt.datetime]
            only downloads registered at or before this time (RFC 3339)

        limit : typing.Optional[int]
            max items per page

        offset : typing.Optional[int]
            pagination offset

        session_id : typing.Optional[str]
            only downloads captured by this session

        sort : typing.Optional[DownloadsListRequestSort]
            field to sort by

        order : typing.Optional[DownloadsListRequestOrder]
            sort direction

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[FileDownloadListResponse]
            OK
        """
        _response = self._client_wrapper.httpx_client.request(
            "downloads",
            method="GET",
            params={
                "q": q,
                "mime_type": mime_type,
                "min_size_bytes": min_size_bytes,
                "max_size_bytes": max_size_bytes,
                "created_after": serialize_datetime(created_after) if created_after is not None else None,
                "created_before": serialize_datetime(created_before) if created_before is not None else None,
                "limit": limit,
                "offset": offset,
                "session_id": session_id,
                "sort": sort,
                "order": order,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    FileDownloadListResponse,
                    parse_obj_as(
                        type_=FileDownloadListResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def delete(
        self, download_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[DeleteFileOutputBody]:
        """
        Removes a captured file from the org's library and everywhere it was delivered: the stored object and the library entry go immediately, and every phone the file was pushed to is scheduled to remove its copy (removal is confirmed per phone and retried until it lands), the same recall an upload's delete runs. The response reports how many phones that recall reaches. The source phone's own copy from the capture session is outside the recall: it belongs to the session, not the library.

        Parameters
        ----------
        download_id : str
            download identifier to delete

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[DeleteFileOutputBody]
            OK
        """
        _response = self._client_wrapper.httpx_client.request(
            f"downloads/{encode_path_param(download_id)}",
            method="DELETE",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    DeleteFileOutputBody,
                    parse_obj_as(
                        type_=DeleteFileOutputBody,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def phones_session_downloads(
        self,
        session_id: str,
        *,
        q: typing.Optional[str] = None,
        mime_type: typing.Optional[str] = None,
        min_size_bytes: typing.Optional[int] = None,
        max_size_bytes: typing.Optional[int] = None,
        created_after: typing.Optional[dt.datetime] = None,
        created_before: typing.Optional[dt.datetime] = None,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[FileDownloadListResponse]:
        """
        Returns the files this session captured off its phone, newest first — the direct answer to "what did this session download". Rows appear at detection, before the bytes finish moving, so a caller waiting on a file watches its capture state progress rather than an empty list.

        Parameters
        ----------
        session_id : str
            session whose captures to list

        q : typing.Optional[str]
            filter by filename, case-insensitive substring match

        mime_type : typing.Optional[str]
            only downloads of exactly this media type

        min_size_bytes : typing.Optional[int]
            only downloads at least this many bytes (0 = no bound)

        max_size_bytes : typing.Optional[int]
            only downloads at most this many bytes (0 = no bound)

        created_after : typing.Optional[dt.datetime]
            only downloads registered at or after this time (RFC 3339)

        created_before : typing.Optional[dt.datetime]
            only downloads registered at or before this time (RFC 3339)

        limit : typing.Optional[int]
            max items per page

        offset : typing.Optional[int]
            pagination offset

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[FileDownloadListResponse]
            OK
        """
        _response = self._client_wrapper.httpx_client.request(
            f"phones/sessions/{encode_path_param(session_id)}/downloads",
            method="GET",
            params={
                "q": q,
                "mime_type": mime_type,
                "min_size_bytes": min_size_bytes,
                "max_size_bytes": max_size_bytes,
                "created_after": serialize_datetime(created_after) if created_after is not None else None,
                "created_before": serialize_datetime(created_before) if created_before is not None else None,
                "limit": limit,
                "offset": offset,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    FileDownloadListResponse,
                    parse_obj_as(
                        type_=FileDownloadListResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawDownloadsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def list(
        self,
        *,
        q: typing.Optional[str] = None,
        mime_type: typing.Optional[str] = None,
        min_size_bytes: typing.Optional[int] = None,
        max_size_bytes: typing.Optional[int] = None,
        created_after: typing.Optional[dt.datetime] = None,
        created_before: typing.Optional[dt.datetime] = None,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        session_id: typing.Optional[str] = None,
        sort: typing.Optional[DownloadsListRequestSort] = None,
        order: typing.Optional[DownloadsListRequestOrder] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[FileDownloadListResponse]:
        """
        Returns one page of the files captured off phones during sessions (AXI-1449), newest first. Each row carries its capture state: a skipped or failed capture is a visible entry with a reason, not an absence. Downloads share the library and its storage quota with uploads, and a ready download can be delivered to a phone by its id like any upload.

        Parameters
        ----------
        q : typing.Optional[str]
            filter by filename, case-insensitive substring match

        mime_type : typing.Optional[str]
            only downloads of exactly this media type

        min_size_bytes : typing.Optional[int]
            only downloads at least this many bytes (0 = no bound)

        max_size_bytes : typing.Optional[int]
            only downloads at most this many bytes (0 = no bound)

        created_after : typing.Optional[dt.datetime]
            only downloads registered at or after this time (RFC 3339)

        created_before : typing.Optional[dt.datetime]
            only downloads registered at or before this time (RFC 3339)

        limit : typing.Optional[int]
            max items per page

        offset : typing.Optional[int]
            pagination offset

        session_id : typing.Optional[str]
            only downloads captured by this session

        sort : typing.Optional[DownloadsListRequestSort]
            field to sort by

        order : typing.Optional[DownloadsListRequestOrder]
            sort direction

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[FileDownloadListResponse]
            OK
        """
        _response = await self._client_wrapper.httpx_client.request(
            "downloads",
            method="GET",
            params={
                "q": q,
                "mime_type": mime_type,
                "min_size_bytes": min_size_bytes,
                "max_size_bytes": max_size_bytes,
                "created_after": serialize_datetime(created_after) if created_after is not None else None,
                "created_before": serialize_datetime(created_before) if created_before is not None else None,
                "limit": limit,
                "offset": offset,
                "session_id": session_id,
                "sort": sort,
                "order": order,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    FileDownloadListResponse,
                    parse_obj_as(
                        type_=FileDownloadListResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def delete(
        self, download_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[DeleteFileOutputBody]:
        """
        Removes a captured file from the org's library and everywhere it was delivered: the stored object and the library entry go immediately, and every phone the file was pushed to is scheduled to remove its copy (removal is confirmed per phone and retried until it lands), the same recall an upload's delete runs. The response reports how many phones that recall reaches. The source phone's own copy from the capture session is outside the recall: it belongs to the session, not the library.

        Parameters
        ----------
        download_id : str
            download identifier to delete

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[DeleteFileOutputBody]
            OK
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"downloads/{encode_path_param(download_id)}",
            method="DELETE",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    DeleteFileOutputBody,
                    parse_obj_as(
                        type_=DeleteFileOutputBody,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def phones_session_downloads(
        self,
        session_id: str,
        *,
        q: typing.Optional[str] = None,
        mime_type: typing.Optional[str] = None,
        min_size_bytes: typing.Optional[int] = None,
        max_size_bytes: typing.Optional[int] = None,
        created_after: typing.Optional[dt.datetime] = None,
        created_before: typing.Optional[dt.datetime] = None,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[FileDownloadListResponse]:
        """
        Returns the files this session captured off its phone, newest first — the direct answer to "what did this session download". Rows appear at detection, before the bytes finish moving, so a caller waiting on a file watches its capture state progress rather than an empty list.

        Parameters
        ----------
        session_id : str
            session whose captures to list

        q : typing.Optional[str]
            filter by filename, case-insensitive substring match

        mime_type : typing.Optional[str]
            only downloads of exactly this media type

        min_size_bytes : typing.Optional[int]
            only downloads at least this many bytes (0 = no bound)

        max_size_bytes : typing.Optional[int]
            only downloads at most this many bytes (0 = no bound)

        created_after : typing.Optional[dt.datetime]
            only downloads registered at or after this time (RFC 3339)

        created_before : typing.Optional[dt.datetime]
            only downloads registered at or before this time (RFC 3339)

        limit : typing.Optional[int]
            max items per page

        offset : typing.Optional[int]
            pagination offset

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[FileDownloadListResponse]
            OK
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"phones/sessions/{encode_path_param(session_id)}/downloads",
            method="GET",
            params={
                "q": q,
                "mime_type": mime_type,
                "min_size_bytes": min_size_bytes,
                "max_size_bytes": max_size_bytes,
                "created_after": serialize_datetime(created_after) if created_after is not None else None,
                "created_before": serialize_datetime(created_before) if created_before is not None else None,
                "limit": limit,
                "offset": offset,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    FileDownloadListResponse,
                    parse_obj_as(
                        type_=FileDownloadListResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        except ValidationError as e:
            raise ParsingError(
                status_code=_response.status_code, headers=dict(_response.headers), body=_response.json(), cause=e
            )
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
